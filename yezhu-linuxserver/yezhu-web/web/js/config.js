// ============ 配置 ============
// API_BASE_URL 三种填法：
//   ''            → 本地预览模式：数据只存在当前浏览器，仅供演示/界面预览。
//   'auto'        → 同源模式：前端由后端一起托管（宝塔一体化部署）时填这个，
//                   用 localhost / 局域网IP / 域名 访问都能自动连上后端，无需改动。
//   'https://你的后端地址'  → 前后端分离部署时，填后端的完整地址。
//
// 宝塔一体化部署（推荐）：把下面填成 'auto'。
window.CONFIG = {
  API_BASE_URL: '',           // 部署后改成 'auto'（或你的后端完整地址）
  APP_NAME: '业主之家',
  // 本小区可选的房屋建筑面积（㎡）。按本楼盘实际户型面积维护，业主在登记时下拉选择。
  AREA_OPTIONS: ['26.47', '26.48', '50.9', '57.51', '57.67', '58.22', '59.78', '59.95'],
  // 指南页内嵌的《工作指导手册》PDF 地址。默认用随包附带的本地文件；
  // 若文件较大，可改为你的对象存储（如阿里云 OSS）链接以省服务器带宽。
  MANUAL_URL: 'docs/longgang-yezhu-handbook.pdf'
}
